

const field = ["key","number", "type", "occupancy", "price", "guest"]
const button = {button: <Button type="primary">More information</Button>,};

const parseToDataTable = (arr) => {
arr.map(key => {} )
}
